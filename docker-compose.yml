services:
  agent:
    build: .
    volumes:
      - ./config:/root/.cc-connect
      - .:/root/agent
      - ./.gemini:/root/.gemini
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
